#include <iostream>
using namespace std; 
int main()
{
    //To find the Reminder we use Modulus sign(%)
    int a,b; 
    cout <<"Enter first Number: "; 
    cin >> a ;
    
    cout <<"Enter Second Number: "; 
    cin >> b;

    cout << "Reminder is : " << a%b; 
}